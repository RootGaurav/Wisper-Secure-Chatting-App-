<UserControl x:Class="Connectt.FriendRequestControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- ========== RESOURCES ========== -->
    <UserControl.Resources>
        <SolidColorBrush x:Key="Ink" Color="#026670"/>
        <SolidColorBrush x:Key="GlassFill" Color="#C0FEF9C7"/>
        <SolidColorBrush x:Key="GlassStroke" Color="#60996670"/>
        <SolidColorBrush x:Key="Warn" Color="#E74C3C"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#43A047"/>
        <SolidColorBrush x:Key="Accent" Color="#43A047"/>
        <SolidColorBrush x:Key="DangerRed" Color="#E53935"/>

        <!-- Glass Header Style -->
        <Style x:Key="HeaderCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="{StaticResource GlassFill}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GlassStroke}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#4066A04B" BlurRadius="16" ShadowDepth="0" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded TextBox with Placeholder -->
        <Style x:Key="RoundedTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="Background" Value="#60FFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource GlassStroke}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="8,0,0,0"/>
                            <TextBlock x:Name="Placeholder"
                                       Text="Enter username..."
                                       Foreground="#808080"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource SuccessGreen}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Buttons -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{StaticResource SuccessGreen}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="10" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="{StaticResource SuccessGreen}"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="{StaticResource DangerRed}"/>
        </Style>

        <Style x:Key="RequestRow" TargetType="ListViewItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,1"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#08FFFFFF"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#16FFFFFF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#22FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!-- ========== MAIN LAYOUT ========== -->
    <Grid Background="Transparent" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Search -->
            <RowDefinition Height="*"/>
            <!-- Horizontal Split -->
        </Grid.RowDefinitions>

        <!-- 🔍 Search -->
        <Border Grid.Row="0" Style="{StaticResource HeaderCard}" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBox x:Name="SearchTextBox"
                         Width="300"
                         Style="{StaticResource RoundedTextBox}"
                         FontSize="14"/>
                <Button x:Name="SendRequestButton"
                        Content="Send Request"
                        Margin="15,0,0,0"
                        Click="SendRequestButton_Click"
                        Style="{StaticResource PrimaryButton}"/>
            </StackPanel>
        </Border>

        <!-- 🤝 Pending + Suggestions side-by-side -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12"/>
                <!-- spacing -->
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 🕒 Pending Requests -->
            <Border Grid.Column="0"
                    CornerRadius="12"
                    Background="#C0FEF9C7"
                    BorderBrush="{StaticResource GlassStroke}"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" CornerRadius="12,12,0,0" Background="{StaticResource Accent}">
                        <TextBlock Text="Pending Friend Requests"
                                   Foreground="White"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Margin="15,10"/>
                    </Border>

                    <ListView x:Name="RequestsListView"
                              Grid.Row="1"
                              BorderThickness="0"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ItemContainerStyle="{StaticResource RequestRow}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,10" BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1">
                                    <Grid VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Name}"
                                                   VerticalAlignment="Center"
                                                   FontSize="14"
                                                   Foreground="{StaticResource Ink}"/>

                                        <Button Grid.Column="1"
                                                Content="Accept"
                                                Style="{StaticResource SuccessButton}"
                                                Tag="{Binding Id}"
                                                Margin="10,0"
                                                Click="AcceptButton_Click"/>

                                        <Button Grid.Column="2"
                                                Content="Deny"
                                                Style="{StaticResource DangerButton}"
                                                Tag="{Binding Id}"
                                                Click="DenyButton_Click"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <!-- 🌟 Friend Suggestions -->
            <Border Grid.Column="2"
                    CornerRadius="12"
                    Background="#C0FEF9C7"
                    BorderBrush="{StaticResource GlassStroke}"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" CornerRadius="12,12,0,0" Background="{StaticResource Accent}">
                        <TextBlock Text="Friend Suggestions"
                                   Foreground="White"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Margin="15,10"/>
                    </Border>

                    <ListView x:Name="SuggestionsListView"
                              Grid.Row="1"
                              BorderThickness="0"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ItemContainerStyle="{StaticResource RequestRow}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,10" BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1">
                                    <Grid VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Padding="10"
                                                   Text="{Binding}"
                                                   VerticalAlignment="Center"
                                                   FontSize="14"
                                                   FontFamily="Arial Black"
                                                   Foreground="{StaticResource Ink}"/>

                                        <Button Grid.Column="1"
                                                Content="Add Friend"
                                                Style="{StaticResource SuccessButton}"
                                                Tag="{Binding}"
                                                Click="AddSuggestion_Click"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
